> [!WARNING]
> The snake game cpu mode goes very fast. If you are photosensitive
> I would not use it without modifying the delay of the program.

# sdl-projects

A collection of projects implemented with SDL3 mostly in C.

## build

```
git clone https://github.com/Icosa-dev/sdl-projects.git
cd sdl-projects
mkdir build
cd build
cmake ..
make

# to run a program assuming you are in the build directory
./<subproject-name>/<subproject-name>
# for example "./snake/snake"
```
