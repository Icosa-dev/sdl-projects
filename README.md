# sdl-projects

A collection of projects implemented with SDL3 mostly in C.

## build

```
# assuming you cloned and are in the project root directory
mkdir build
cd build
cmake ..
make

# to run a program assuming you are in the build directory
./<project-name>/<project-name>
```
